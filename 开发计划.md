# 开发计划与进度

> 开发者：**彭云**（单人开发）
> 相关：里程碑总览见 `V2_ROADMAP.md` ｜ 开发规范见 `CONTRIBUTING.md` ｜ 接口契约见 `接口契约.md`

**最后更新：2026-09-14**

---

## 一、进度总览

| 里程碑 | 状态 | 内容 |
|---|---|---|
| **M1 骨架** | ✅ | FastAPI + LangGraph 单 Agent + 7 个高德工具（限流/重试/异常护栏） |
| **M2 前端** | ✅ | Vue3 四栏：高德地图 / 对话 / 行程 / Agent 轨迹 + `?demo=1` 演示模式 |
| **M3 多 Agent** | ✅ | Researcher → Critic ⇄ Reviser → Planner；结构化行程 JSON；审查报告卡；修订白名单核验 |
| **工程基建** | ✅ | git 仓库 + CI（语法/构建/密钥扫描）+ 本地 pre-commit 钩子 + 接口契约冻结 |
| **M4 运筹优化** | ⬜ | OR-Tools VRPTW 单日最优排序 + 前后对比 |
| **M5 主动感知** | ⬜ | 事件流（天气/闭馆）+ 自动重排 + 前端推送 |
| **M6 记忆/观测/评估** | ⬜ | SQLite 持久化 + 埋点面板 + 场景回归套件 |
| **M7 交付** | ⬜ | README 重写 + 架构图 + 3 分钟演示视频 |

---

## 二、接下来做 M4（运筹优化）

**为什么先做这个**：它是全项目唯一能用数字说话的差异化（「同一天总里程降 30%」），而且边界清晰——只新增一个文件加一处编排接入，不会把现有链路改坏。

### 要做的事

1. **拉时间矩阵**
   - 取 `TripPlan` 里某一天的地点序列（`place.location` 已是 `lng,lat`）
   - 两两算路径耗时。**注意调用量**：n 个点要 n²−n 次，5 个点就是 20 次——个人 Key 约 3 QPS，必须复用 `tools/amap.py` 的限流锁（或走高德批量距离接口减少调用）
   - 结果做成矩阵缓存起来，别重复请求同一对点

2. **建 VRPTW 模型**（OR-Tools `pywrapcp`）
   - 节点：起点（酒店/出发点）+ 各 POI + 终点
   - `Time` 维度：POI 营业时间做时间窗（`open_time` 解析出 `[最早, 最晚]`），`duration_min` 做服务时长
   - `Distance` 维度：路径耗时做 ArcCost
   - 首解用 `PATH_CHEAPEST_ARC`，再用 `GUIDED_LOCAL_SEARCH` 限定 5 秒内求解

3. **回写与展示**
   - 重排后的顺序 + 建议到达时间写回 `TripPlan`（`DayItem.time`），Markdown 一并更新
   - 前端行程面板加「优化前 / 优化后」对比条：总里程、总耗时、下降百分比

### 验收标准

- [ ] 同一天 ≥4 个点能生成合法序列，**所有点落在营业时间内**
- [ ] 总里程（或总耗时）相比初版下降 **≥10%**
- [ ] 矩阵构造走的限流锁，不出现高德 `10021`
- [ ] 求解时间 **< 10 秒**（超时降级返回原顺序，不能把整个请求拖死）
- [ ] 前端能看到对比数字

### 落地位置

- 新增 `v2/backend/agents/optimizer.py`
- `main.py` 在 Critic 环节之后、Planner 之前插入优化（或 Planner 之后对 `TripPlan` 优化后重新渲染）
- `requirements.txt` 加 `ortools`

---

## 三、之后依次做

### M5 主动感知（做完 M4 再动）

最小可用版，不需要 Kafka：

- 行程状态落 SQLite（顺便解决 M1 遗留的「重启丢上下文」）
- `POST /api/trip/check`：体检接口，输入行程 → 输出「发现的问题」
- 事件源：`weather_forecast` 轮询 + 闭馆规则表（周一闭馆类）
- 命中 → 自动 Critic → Reviser → SSE 推 `trip_update` → 前端提示条 + 高亮变化项
- **验收**：模拟「明天有雨」，能自动把户外项换成室内项并在前端弹出提示

### M6 记忆 / 观测 / 评估

- SQLite checkpointer 换掉 InMemorySaver
- 埋点：每次请求记录工具调用数 / 耗时 / token 成本，前端加小面板
- **场景回归套件**：把 `test_reviser_whitelist.py` 的模式推广成 `scenarios/*.yaml` + 一键跑分脚本
  - 指标：Critic 硬伤拦截率、平均耗时、工具调用数
  - 这份报告本身就是「有评估体系」的证据

### M7 交付

- README 重写：架构图 + 演示 GIF + 量化指标表
- 3 分钟演示视频（脚本见下）
- 演示视频脚本：输入需求 → 地图 POI 亮起 → Critic 审查卡 → 优化前后对比 → 行程体检弹提示 → 导出

---

## 四、单人开发的取舍（明确不做）

时间有限，下面这些**列进「未来工作」就行，不要动手**：

| 不做 | 原因 |
|---|---|
| 多模态（语音 / 拍照识别） | 演示时不是关键镜头，投入产出比低 |
| ChromaDB 长期向量记忆 | SQLite 存偏好已经够演示 |
| MCP 协议接入 | 概念加分但工程量大，与核心差异化无关 |
| Docker / K8s 部署 | 答辩现场本地跑更快更稳；想要公网链接用静态部署即可 |
| 多用户并发 / 权限系统 | 单人演示项目不需要 |

**判断标准**：一个功能如果能出现在演示视频的 3 分钟里，或者能变成一个可写的数字，才做。

---

## 五、当前卡点与待办

| 事项 | 状态 |
|---|---|
| 推送到 GitHub（凭据需本机交互登录） | ⬜ 待执行 |
| 高德控制台给 JSAPI Key 绑域名白名单 | ⬜ 待执行 |
| Planner 结构化输出偶发失败 | 🟡 已加一次重试，观察中 |
| 一次真实请求 60–120 秒 | 🟡 M4/M6 时一并优化 |
| Critic 用 flash 有时把「风险」判成 high | 🟡 已修正判定规则措辞，需多跑几次观察 |

---

## 六、每周回顾（自己填）

**9.14–9.20**
- 做了：
- 卡在：
- 下周：

**9.21–9.27**
- 做了：
- 卡在：
- 下周：
