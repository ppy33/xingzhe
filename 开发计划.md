# 开发计划与进度

> 开发者：**彭云**（单人开发）
> 相关：里程碑总览见 `V2_ROADMAP.md` ｜ 开发规范见 `CONTRIBUTING.md` ｜ 接口契约见 `接口契约.md`

**最后更新：2026-09-14**

---

## 一、进度总览

| 里程碑 | 状态 | 内容 |
|---|---|---|
| **M1 骨架** | ✅ | FastAPI + LangGraph 单 Agent + 7 个高德工具（限流/重试/异常护栏） |
| **M2 前端** | ✅ | Vue3 四栏：高德地图 / 对话 / 行程 / Agent 轨迹 + `?demo=1` 演示模式 |
| **M3 多 Agent** | ✅ | Researcher → Critic ⇄ Reviser → Planner；结构化行程 JSON；审查报告卡；修订白名单核验 |
| **工程基建** | ✅ | git 仓库 + CI（语法/构建/密钥扫描）+ 本地 pre-commit 钩子 + 接口契约冻结 |
| **M4 运筹优化** | ✅ | OR-Tools VRPTW 单日最优排序 + 坐标体检 + 前后对比卡 |
| **M5 主动感知** | ✅ | 行程体检（闭馆/雨天/营业时间）+ 自动修复 + 前端提示条；SQLite 行程库 + 对话记忆落盘 |
| **M6 记忆/观测/评估** | 🟡 | SQLite 已随 M5 完成；剩埋点面板 + 场景回归套件 |
| **M7 交付** | ⬜ | README 重写 + 架构图 + 3 分钟演示视频 |

---

## 二、M4 完成情况（运筹优化）

新增 `agents/optimizer.py`，主流程变为 Researcher → Critic ⇄ Reviser → Planner → **Optimizer**。

### 做了什么

1. **时间矩阵**：走高德 `/v3/distance` 批量接口，n 个点只要 n 次调用（不是 n²-n 次）；
   复用 `tools/amap.py` 的限流锁；进程内缓存 `(origin,dest) → (分钟,公里)`
2. **VRPTW 建模**：单车辆 + `Time` 维度（营业时间窗 + 停留时长）+ 弧成本 = 通行耗时；
   `PATH_CHEAPEST_ARC` 首解 → `GUIDED_LOCAL_SEARCH`，限时 2.5 秒（OR-Tools 阻塞调用放线程池）
3. **三级降级**：严格（到达+停留 ≤ 关门）→ 宽松（到达 ≤ 关门）→ 等比压缩停留时长排序（不回写），
   全失败才保原顺序
4. **回写与展示**：重排 `DayItem.order/time/transport_to_next`，前端加「优化前 / 优化后」对比卡
   （总耗时、总里程、下降百分比、逐日顺序变化）

### 顺带修掉的隐藏 bug（比优化本身更值钱）

| bug | 影响 | 修法 |
|---|---|---|
| Planner 会**编造坐标**（锦里写到河北邢台、宽窄巷子写到顺义） | 距离矩阵失真 100 倍 → 优化必然无解；前端地图标到别的省 | 坐标体检：以「工具轨迹里的高德真实坐标」为权威源覆盖，其次按名重查 |
| 工具结果被截断到 2000 字符 → JSON 解析失败 | 拿不到权威坐标 | 正则抠 `"name"…"location"` 对，不依赖完整 JSON |
| 用行程里的坐标投票推城市 | 坏点占多数会推出假城市（实测「廊坊市」），把好坐标全改坏 | 没有权威参照时只补空坐标，不乱动已有坐标 |
| `plan_to_struct` 偶发返回 `None` | 曾导致 `plan_json` 偶发缺失 | 显式校验 + 最多重试 3 次（后两次强提醒必须调工具） |
| `time_limit.FromSeconds(2.5)` | 抛 `'float' object cannot be interpreted as an integer` | 改用 `FromMilliseconds` |
| `manager.NodeToIndex(end)` 返回 -1 | `CumulVar(-1)` 直接段错误 | 终点节点用 `routing.End(0)` |

### 验收结果

| 验收项 | 结果 |
|---|---|
| ≥4 个点生成合法序列且落在营业时间内 | ✅ 离线用例 + 真实请求均无 violations |
| 总里程/耗时相比初版下降 ≥10% | ✅ 真实数据 7 点日：80.9 → 72.5 分钟（**10.3%**）；另一例 62.2 → 56.0（10.0%） |
| 矩阵构造走限流锁、无高德 10021 | ✅ 全程复用 `_amap_get` |
| 求解 < 10 秒，超时降级 | ✅ 求解 2.5 秒上限，含矩阵构建约 4.6 秒；无解自动保原顺序 |
| 前端能看到对比数字 | ✅ 「⚡ 路线已优化 · 省 X%」卡片（可折叠，含逐日顺序对比） |

回归脚本：`backend/test_optimizer.py`（8 个用例，离线，不消耗高德配额）

---

## 二·五、M5 完成情况（主动感知）

新增 `agents/inspector.py` + `storage.py`，行程生成后可**主动体检**并自动修复。

### 做了什么

1. **存储层** `storage.py`：行程 + 事件落 SQLite（`data/xingzhe.db`），重启后同一 `thread_id` 可取回行程继续追问；顺带把 Researcher 的 `InMemorySaver` 换成 `AsyncSqliteSaver`，解决「重启丢上下文」
2. **体检器** `inspector.py`：三类规则巡检（闭馆规则表 / 雨天户外 / 营业时间越界），输出结构化 `TripInspection`
3. **自动修复**：命中 issue 时调 fixer Agent（flash + 结构化输出），雨天户外项从高德检索的室内候选点替换；`diff_plans()` 产出 `PlanChange[]`（before/after/reason/added），新增地点带审计标记
4. **接口**：`POST /api/trip/check`（非流式）+ `/api/trip/check/stream`（SSE：check_start → step → issue×N → trip_update → plan_json → optimize → done）
5. **前端**：PlanPanel 加「行程体检」入口 + 问题提示条 + 变化项高亮，AgentTrace 加 insp_weather/insp_rules/insp_fix 标签

### 踩坑（M5 新增）

| 坑 | 影响 | 修法 |
|---|---|---|
| fixer 也犯 Planner 老毛病：不调工具直接回自然语言 | `fix_trip` 返回 None，体检修复静默失效 | 显式校验 + 重试 |
| 城市推断失灵（行程 `address` 常为空） | `guess_city` 推不出城市，天气/室内候选查错地方 | 新增 `infer_city()`：用 POI 坐标 `regeo` 反查城市兜底 |

### 验收结果

| 验收项 | 结果 |
|---|---|
| 模拟「明天有雨」自动换室内项 | ✅ 暴雨场景抓到 3 个雨天户外 issue，自动换成美术馆/商圈，`changes` 带 `added` 审计标记 |
| 前端弹出提示 + 高亮变化项 | ✅ demo 模式截图 `docs/m5_check_card.png`；体检卡 + `.sp-item--changed` 高亮 |
| 体检失败不拖垮主流程 | ✅ 天气接口失败跳过天气维度；fixer 失败保留原行程 `fixed=false` |
| 离线回归 | ✅ `backend/test_inspector.py`（8 个用例，不联网） |

回归脚本：`backend/test_inspector.py`

---

## 三、之后依次做

### M6 记忆 / 观测 / 评估

- ~~SQLite checkpointer 换掉 InMemorySaver~~ ✅ 已随 M5 完成
- 埋点：每次请求记录工具调用数 / 耗时 / token 成本，前端加小面板
- **场景回归套件**：把 `test_reviser_whitelist.py` 的模式推广成 `scenarios/*.yaml` + 一键跑分脚本
  - 指标：Critic 硬伤拦截率、平均耗时、工具调用数
  - 这份报告本身就是「有评估体系」的证据

### M7 交付

- README 重写：架构图 + 演示 GIF + 量化指标表
- 3 分钟演示视频（脚本见下）
- 演示视频脚本：输入需求 → 地图 POI 亮起 → Critic 审查卡 → 优化前后对比 → 行程体检弹提示 → 导出

---

## 四、单人开发的取舍（明确不做）

时间有限，下面这些**列进「未来工作」就行，不要动手**：

| 不做 | 原因 |
|---|---|
| 多模态（语音 / 拍照识别） | 演示时不是关键镜头，投入产出比低 |
| ChromaDB 长期向量记忆 | SQLite 存偏好已经够演示 |
| MCP 协议接入 | 概念加分但工程量大，与核心差异化无关 |
| Docker / K8s 部署 | 答辩现场本地跑更快更稳；想要公网链接用静态部署即可 |
| 多用户并发 / 权限系统 | 单人演示项目不需要 |

**判断标准**：一个功能如果能出现在演示视频的 3 分钟里，或者能变成一个可写的数字，才做。

---

## 五、当前卡点与待办

| 事项 | 状态 |
|---|---|
| 推送到 GitHub（凭据需本机交互登录） | ⬜ 待执行 |
| 高德控制台给 JSAPI Key 绑域名白名单 | ⬜ 待执行 |
| Planner 结构化输出偶发失败 | ✅ 已定位根因（模型不调工具返回 None）+ 三次重试加固 |
| 一次真实请求 60–120 秒 | 🟡 M4 矩阵/求解约 +5 秒，仍是 M6 优化项 |
| Critic 用 flash 有时把「风险」判成 high | 🟡 已修正判定规则措辞，需多跑几次观察 |
| LLM 排的顺序偶尔本就不差，优化降幅为 0 | 🟡 属正常（无需优化）；降幅数字只在乱序输入下才明显，做演示时可以给定乱序 |

---

## 六、每周回顾（自己填）

**9.14–9.20**
- 做了：
- 卡在：
- 下周：

**9.21–9.27**
- 做了：
- 卡在：
- 下周：
