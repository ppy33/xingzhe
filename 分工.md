# 协作分工

> 团队：用户（主导开发）· 许涵松（原型 / 架构顾问）
> 认领任务请开 Issue 用「任务认领」模板，完成后更新下表。
> 协作规范见 `CONTRIBUTING.md`，接口契约见 `接口契约.md`。

**最后更新：2026-09-14**

---

## 团队

| 角色 | 姓名 | 责任 |
|---|---|---|
| 主导开发 | 用户 | v2 后端（FastAPI + LangGraph 多 Agent）、前端（Vue3）、CI 与协作基建 |
| 原型与架构 | 许涵松 | v1 原型（`行者.html`）、Agent 循环设计、Step 协议、运筹思路 |

---

## 当前进度（v2）

| 里程碑 | 状态 | 负责人 | 产出 |
|---|---|---|---|
| **M1 骨架** | ✅ 完成 | 用户 | FastAPI + LangGraph 单 Agent + 7 个高德工具（`v2/backend`） |
| **M2 工具迁移 + 前端** | ✅ 完成 | 用户 | 四栏前端：地图 / 对话 / 行程 / Agent 轨迹（`v2/frontend`） |
| **M3 多 Agent 闭环** | ✅ 完成 | 用户 | Researcher → Critic ⇄ Reviser → Planner；结构化 JSON + 审查卡 + 抗幻觉核验 |
| **协作基建** | ✅ 完成 | 用户 | git 仓库 + PR 流程 + CI（含密钥扫描）+ 接口契约冻结 |
| **M4 运筹优化** | ⬜ 待认领 | — | OR-Tools VRPTW 单日最优排序 + 前后对比 |
| **M5 主动感知** | ⬜ 待认领 | — | 事件流（天气/闭馆）+ 自动重排 + 前端推送 |
| **M6 记忆 / 观测 / 评估** | ⬜ 待认领 | — | SQLite 持久化 + 埋点面板 + 场景回归套件 |
| **M7 文档与演示** | ⬜ 待认领 | — | README 重写 + 架构图 + 3 分钟演示视频 |

---

## 可认领的工作（按适合度排序）

### 方向 A · M4 运筹优化（OR-Tools VRPTW）⭐ 最推荐

**为什么推荐**：这是 v2 相对参考项目的核心差异化，产出能用数字说话（「总里程降低 30%」），写进材料最有说服力；而且边界清晰、不碰别人的代码。

- 新增 `v2/backend/agents/optimizer.py`：输入 `TripPlan` 的单日地点序列 → 输出最优顺序
- 关键要素：高德两两路径耗时构造时间矩阵、POI 营业时间做时间窗、`duration_min` 做服务时长
- 输出回写 `TripPlan`，前端加「优化前 / 优化后」对比条
- **验收**：同一天 ≥4 个点，总里程下降 ≥10%，且所有点落在营业时间内

### 方向 B · 数据采集与清洗

**为什么适合**：独立性强，不依赖后端跑通，纯 Python + 数据能力，适合先上手熟悉项目。

- 用高德 POI API 写采集脚本，把示例数据扩充到 500+ POI
- 清洗对齐 `DATA.md` 的字段规范（去重、补全营业时间、补坐标）
- 产出：`data/poi_extended.json` + `scripts/poi_crawler.py`

### 方向 C · M6 评估与观测

**为什么适合**：方法论清晰（跑场景 → 算指标 → 出报告），也是简历上「有评估体系」的证据。

- 把 `v2/backend/test_reviser_whitelist.py` 的模式推广成场景回归套件（`scenarios/*.yaml` + 一键跑分）
- 指标：Critic 硬伤拦截率、平均耗时、工具调用数、token 成本
- 产出：`eval/` 目录 + 评估报告

### 方向 D · M5 主动感知

**门槛稍高**（要动主流程 + 前端推送），建议在 M4 完成、对代码熟悉后再认领。

- 行程状态持久化（SQLite）+ `POST /api/trip/check` 体检接口
- 事件源：天气轮询 + 闭馆规则表
- 命中后自动 Critic → Reviser → SSE 推送 `trip_update` → 前端提示条

---

## 认领表

| 模块 | 负责人 | 状态 | 开始日期 | 完成日期 |
|---|---|---|---|---|
| M4 运筹优化 | （待认领） | ⬜ | — | — |
| 数据采集与清洗 | （待认领） | ⬜ | — | — |
| M6 评估与观测 | （待认领） | ⬜ | — | — |
| M5 主动感知 | （待认领） | ⬜ | — | — |

---

## 进度同步

**9.13（已完成）**
- ✅ 用户：v2 骨架 → 7 工具 → Vue3 前端 → 多 Agent 闭环，一天跑通 M1–M3
- ✅ 双方：定 git 仓库（GitHub 私有）+ 分支/PR 流程 + 冻结接口契约

**9.14–9.20（本周）**
- ⬜ 认领上方方向，开 Issue 登记
- ⬜ 拉代码、配 `.env`、本地跑通前后端（见 `CONTRIBUTING.md` 第一节）
- ⬜ 第一周目标：M4 出可跑版本

**9.21–9.27**
- ⬜ ...

---

## 沟通约定

- **代码改动**：各自开 `feat/xxx` 分支 → 提 PR → 至少 1 人 review → squash 合并（细则见 `CONTRIBUTING.md`）
- **契约变更**：改 `schemas.py` / SSE 事件 / REST 字段前，先开 Issue 说明理由，双方确认后同一个 PR 内同步后端 + 前端 + `接口契约.md`
- **架构变动**：先开 Issue，别先写代码
- **卡住超过 2 小时**：直接问
- **周会**：每周日晚 20:00，线上 15 分钟（各自说「做了啥 / 卡在哪 / 下周做啥」）
- **紧急事**：微信直说，事后补 Issue 留痕

---

## 共享资源

- 仓库：GitHub 私有（见微信发送的邀请链接）
- 后端入口：`v2/backend/main.py` ｜ 前端入口：`v2/frontend/src/App.vue`
- 契约：`接口契约.md` ｜ 协作规范：`CONTRIBUTING.md`
- 运行方式：见 `CONTRIBUTING.md` 第一节（含 Windows 代理坑）
- 演示截图：`v2/docs/*.png`
- v1 原型参考：`行者.html`（只读，不再修改）

---

## 风险与依赖

| 风险 | 影响 | 应对 |
|---|---|---|
| ⛔ DeepSeek API 费用 | 真实链路测试烧钱 | 改 UI 用 `?demo=1`；后端调试优先 `?demo` 与离线脚本 |
| ⛔ 高德 Key 配额（个人 Key 约 3 QPS） | 并发调用报 10021 | 复用 `tools/amap.py` 的限流锁；M4 构造时间矩阵时注意调用量 |
| ⛔ 前端 Key 未绑域名白名单 | 配额被他人白嫖 | 去高德控制台绑定白名单（**待办**） |
| ⛔ 部署平台未定 | M7 无法出公网链接 | 本地 demo 优先，部署留到最后一周决策 |
| ⛔ 一次真实请求 60–120 秒 | 演示节奏拖沓 | M4/M6 优化耗时；演示视频可剪辑加速 |
