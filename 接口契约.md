# 接口契约 v1.1

> **这是前后端共同依赖的契约文件。改这里之前先读第六节「变更流程」。**
>
> 契约版本：**v1.1** ｜ 更新日期：2026-09-14 ｜ 对应代码：`v2/backend` @ M4 完成态

---

## 变更日志

| 版本 | 日期 | 变更 | 影响面 |
|---|---|---|---|
| v1.0 | 2026-09-14 | 首次冻结：REST 3 接口、SSE 7 事件、8 个 Pydantic Schema | 全量 |
| v1.1 | 2026-09-14 | **M4**：新增 SSE `optimize` 事件、`/api/chat` 响应新增 `optimize` 字段、新增伪工具 `optimizer_route`、新增第四节附「`optimize` 对象」；`error` 事件 `message` 改为 `异常类型: 描述` | 加字段/加事件，**不破坏 v1.0 兼容性**（前端忽略未知事件即可） |

---

## 一、REST 接口

### `GET /health`

健康检查 + 密钥配置状态（前端启动时探测用）。

```json
{
  "status": "ok",
  "amap_key_configured": true,
  "llm_configured": true,
  "model": "deepseek-v4-pro"
}
```

### `POST /api/chat`

单轮对话，返回完整结果（含结构化行程）。**不流式，前端要等 60–120 秒。**

**请求**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `message` | string | ✅ | 用户自然语言输入，空字符串返回 400 |
| `thread_id` | string | ❌ | 会话 ID，不传自动生成。相同 ID 视为同一会话（Agent 有记忆） |

**响应**

| 字段 | 类型 | 说明 |
|---|---|---|
| `thread_id` | string | 会话 ID，前端下次追问时带上 |
| `answer` | string | 最终行程 Markdown。**若 Planner 成功，这里是 `TripPlan.to_markdown()` 的结果**；若 Planner 失败，这里是 Researcher/Critic 的原始 Markdown |
| `steps` | Step[] | 完整执行轨迹（见第三节） |
| `plan_json` | object \| null | `TripPlan` 的序列化结果，Planner 失败时为 `null`。**坐标已过体检**（见第四节附 `days[].geo`） |
| `review` | object \| null | Critic 审查报告（见第四节），审查环节整体失败时为 `null` |
| `optimize` | object \| null | M4 运筹优化前后对比（见第四节附），Planner 失败或优化异常时为 `null` |

### `POST /api/chat/stream`

SSE 流式接口，前端「Agent 轨迹」面板靠它实时滚动。请求体同 `/api/chat`。

事件顺序（正常情况）：

```
start → step×N → review → step×M → optimize → plan_json → token → done
                        ↑
                  也可能出现 error 并提前结束
```

---

## 二、SSE 事件

格式统一为 `event: <类型>\ndata: <JSON>\n\n`，JSON 一律 `ensure_ascii=False`（中文原样）。

| 事件 | data 字段 | 说明 |
|---|---|---|
| `start` | `thread_id` | 开始处理 |
| `step` | `type`, `tool`, `args` 或 `content` | 工具调用轨迹，见第三节 |
| `review` | `passed`, `issue_count`, `summary`, `issues[]`, `rounds`, `revisions`, `new_places[]` | **Critic 审查报告**，在 Critic 环节结束后发一次 |
| `optimize` | `applied`, `solver`, `solve_ms`, `before_min`, `after_min`, `before_km`, `after_km`, `saving_pct`, `days[]` | **M4 优化前后对比**，在 Planner 之后发一次。`applied=false` 时带 `reason` |
| `plan_json` | 完整 `TripPlan` 对象 | 结构化行程，前端据此渲染「结构化」视图。Planner 失败时**不发**此事件 |
| `token` | `content` | 最终答案全文（一次性发送，不是逐字流） |
| `done` | `thread_id` | 正常结束 |
| `error` | `message` | 出错，前端应展示 `message`。**格式为 `异常类型: 描述`**（便于排查空描述异常） |

> **前端必须容忍事件缺失**：`review`、`optimize`、`plan_json` 都可能因上游失败而不出现。
> 都不发时，仍应能靠 `token` 渲染 Markdown 行程。

---

## 三、`step` 事件与工具名

`step` 有两种形态：

```json
{"type": "tool_call",   "tool": "poi_search", "args": {"keywords": "宽窄巷子", "city": "成都"}}
{"type": "tool_result", "tool": "poi_search", "content": "{\"count\":3,...}"}
```

`content` 是**字符串**（内部是 JSON 文本），前端展示前自行截断。

### 工具名清单

**真实高德工具**（来自 `tools/amap.py`，Researcher 调用）：

`poi_search` · `poi_around` · `poi_detail` · `weather_now` · `weather_forecast` · `geo_geocode` · `route_plan`

**流程伪工具**（不是真工具，是编排层产生的假步骤，前端用来展示「哪个 Agent 在干活」）：

| 伪工具名 | args | content | 前端展示为 |
|---|---|---|---|
| `critic_review` | `{round, focus}` | `{round, passed, issue_count, summary, issues[]}` | Critic 审查 |
| `revise_plan` | `{round, issues}` | `{round, chars, new_places[]}` | 行程修订 |
| `planner_struct` | `{schema}` | `{days, items, budget_items, tips}` 或 `{error}` | 结构化规划 |
| `optimizer_route` | `{solver, objective, time_limit_ms}` | `{applied, days, before_min, after_min, saving_pct, solve_ms}` 或 `{error}` | 路线优化 |

> 前端若遇到**未知工具名**，不要崩，降级为显示原始名字。

---

## 四、`review` 对象

```json
{
  "passed": true,
  "issue_count": 5,
  "summary": "行程整体合理、预算在 600 元内……",
  "issues": [
    {"dimension": "time",   "severity": "high",   "description": "兵马俑周一闭馆……"},
    {"dimension": "budget", "severity": "low",    "description": "预算未计入往返大交通……"}
  ],
  "rounds": 2,
  "revisions": 1,
  "new_places": []
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `passed` | bool | 是否通过审查。**规则：只要存在任一 `high` 问题，必须为 `false`** |
| `issue_count` | int | 问题总数 |
| `summary` | string | 一句话总评（截断至 120 字） |
| `issues[]` | object[] | 最多 6 条 |
| `issues[].dimension` | string | 枚举：`weather` / `budget` / `time` / `geo` / `other` |
| `issues[].severity` | string | 枚举：`high` / `medium` / `low` |
| `issues[].description` | string | 问题描述（截断至 100 字） |
| `rounds` | int | 实际审查轮数（上限 2） |
| `revisions` | int | 实际修订轮数（0 或 1） |
| `new_places` | string[] | ★ **修订审计**：修订稿中出现、但初版没有的地点。非空说明 Reviser 未经工具核实补充过地点，前端需显著提示用户 |

---

## 四附、`optimize` 对象（M4 新增）

```json
{
  "applied": true,
  "solver": "ortools-tspTW",
  "solve_ms": 5465,
  "before_min": 83, "after_min": 74, "saving_pct": 14.1,
  "before_km": 12.2, "after_km": 10.9,
  "days": [
    {
      "date": "2026-09-14", "points": 7, "applied": true, "reordered": true, "mode": "strict",
      "before_min": 83, "after_min": 71.2, "saving_pct": 14.2,
      "before_km": 12.2, "after_km": 10.9,
      "order_before": ["春熙路步行街", "成都太古里", "..."],
      "order_after":  ["春熙路步行街", "东郊记忆", "..."],
      "geo": {"city": "成都市", "from_tools": ["武侯祠"], "regenerated": [], "outliers": []}
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `applied` | bool | 是否至少优化了一天。`false` 时看 `reason` 或逐日 `reason` |
| `solver` | string | 恒为 `ortools-tspTW` |
| `solve_ms` | int | 各天求解耗时合计（含距离矩阵构建） |
| `before_min` / `after_min` | float | 优化前 / 后**当日点位之间的通行耗时**合计（分钟，不含停留时长） |
| `before_km` / `after_km` | float | 同上，单位公里 |
| `saving_pct` | float | `(before_min - after_min) / before_min × 100`，保留 1 位 |
| `days[]` | object[] | 逐日明细 |
| `days[].points` | int | 该天参与优化的地点数（含坐标的地点） |
| `days[].mode` | string | 求解模式：`strict`（到达+停留 ≤ 关门）/ `loose`（仅要求到达 ≤ 关门）/ `compressed`（等比压缩停留时长，**不回写行程**） |
| `days[].order_before` / `order_after` | string[] | 地点名顺序，前端用来展示「顺序怎么变的」 |
| `days[].geo` | object | 坐标体检结果：`city` 推断城市、`from_tools` 用工具轨迹坐标修正的点、`regenerated` 按名重查的点、`outliers` 判定为可疑的点 |
| `days[].reason` | string | 未优化原因（如「地点不足 4 个，未优化」） |

> **前端约定**：`applied=false` 时不要报错或隐藏卡片，应展示 `reason`（如实告知「这次没优化，原因是……」）。

---

## 五、Pydantic Schema

源码：`v2/backend/agents/schemas.py`。以下字段表即契约，前端 TypeScript 类型应与之一一对应。

### `TripPlan`

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `title` | string | 必填 | 行程标题，如「上海 → 成都 3 天 2 晚」 |
| `overview` | string | `""` | 总览 2–4 句 |
| `days` | `Day[]` | `[]` | 每日安排 |
| `transportation` | string | `""` | 交通建议整段 |
| `budget` | `BudgetItem[]` | `[]` | 分项预算 |
| `tips` | `string[]` | `[]` | 温馨提示 |

### `Day`

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `date` | string | 必填 | `YYYY-MM-DD` |
| `weekday` | string | `""` | 周几，如 `"二"` |
| `weather` | string | `""` | 如 `"晴 17~28℃"` |
| `theme` | string | `""` | 当天主题 |
| `items` | `DayItem[]` | `[]` | 当天安排项 |
| `summary` | string | `""` | 当天小结 |

### `DayItem`

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `time` | string | 必填 | 开始时间 `HH:MM` |
| `title` | string | 必填 | 一句话标题 |
| `place` | `PlaceRef \| null` | `null` | 关联地点，交通段可为空 |
| `duration_min` | int \| null | `null` | 预计停留分钟数 |
| `notes` | string | `""` | 备注 / 玩法 |
| `transport_to_next` | string | `""` | 到下一项的交通方式与耗时 |

### `PlaceRef`

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `name` | string | 必填 | 地点名（约定来自工具返回） |
| `category` | string | `"other"` | 枚举：`sight` / `food` / `stay` / `transit` / `other` |
| `address` | string | `""` | 地址 |
| `location` | string | `""` | `"lng,lat"`，**空字符串时前端地图应跳过该点**。M4 起坐标经过体检：优先用高德工具轨迹里的真实坐标覆盖 LLM 可能编造的值；核实不了的点会被置空 |
| `open_time` | string | `""` | 营业时间原文 |
| `cost` | float \| null | `null` | 人均花费（元） |
| `rating` | string | `""` | 评分 |

### `BudgetItem`

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `category` | string | 必填 | 分项名，如「往返高铁」 |
| `amount` | string | 必填 | 金额字符串（**不是数字**），如 `"1800 元/人"` |

### `ReviewReport` / `ReviewIssue` / `WhitelistCheck`

对应第四节的 `review` 对象。`WhitelistCheck` 是服务端内部使用的白名单核验结构，**前端不消费**。

---

## 六、变更流程（改契约前必读）

| 变更类型 | 允许？ | 怎么做 |
|---|---|---|
| **加字段** | ✅ 随时 | 必须给默认值（`Field(default=...)`），并在本文件补一行 |
| **加 SSE 事件类型** | ✅ 随时 | 前端必须能忽略未知事件；本文件补一行 |
| **加伪工具名** | ✅ 随时 | 前端未知工具名要降级显示，不能崩 |
| **改字段名 / 删字段 / 改事件名 / 改枚举值** | ❌ 禁止直接改 | 先在 issue 里说明「为什么非改不可」→ 双方确认 → **同一个 PR** 里同时改后端 + 前端 + 本文件 |
| **改 `answer` 的语义**（当前是「Planner 成功则用渲染后的 Markdown」） | ⚠️ 慎改 | 前端多处依赖，必须双方确认 |

**每次契约变更**：本文件顶部版本号 +0.1（如 v1.0 → v1.1），并在「变更日志」表里记一行。

---

## 七、稳定性承诺

| 环节 | 失败时的行为（前端可依赖） |
|---|---|
| Researcher 失败 | 抛 500，`detail` 含可读错误 |
| Critic 失败 | 跳过审查，直接放行当前版本，`review` 为 `null` |
| Reviser 失败 | 保留修订前的版本，继续流程 |
| 白名单核验失败 | 跳过核验（不阻塞主流程），`new_places` 为空 |
| Planner 失败 | 最多重试 3 次（后两次追加「必须调用工具」强提醒）；仍失败则 `plan_json` 为 `null`，`answer` 回落到 Markdown |
| 坐标体检失败 | 该点坐标置空（地图跳过）；不影响其余点位 |
| Optimizer 失败/无解/点数不足 | 跳过优化，行程保持 Planner 的顺序，`optimize.applied=false` 并带 `reason` |
| 距离矩阵接口失败 | 该列退化为直线距离 ×1.4 ÷ 25km/h 估算，不中断 |

> 换句话说：**任何单点失败都不会让整个请求失败**（Researcher 除外）。前端必须按「`plan_json` / `review` / `optimize` 可能为 null」来写。
